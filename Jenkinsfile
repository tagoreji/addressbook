pipeline {
    agent any

    // tools {
    //     // Install the Maven version configured as "M3" and add it to the path.
    //     // maven "M3"
    // }

    stages {
        stage('Compile') {
            steps {
                script{
                    echo "compiling code"
    
                }
            }
        }
        
                stage('unit_test') {
            steps {
                script{
                        echo "testing code"

    
                }
            }
        }
                stage('package') {
            steps {
                script{
                        echo "packaging code"

    
                }
            }
        }
        
    }
}
